@model XsdTransformer.Web.Site.Models.CsvTransformViewModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <title>title</title>
</head>
<body>
    <div>
        <div class="form-row">
            <div class="form-col">
                @Html.TextAreaFor(x => x.Csv, new { rows = 60, cols = 200, wrap = "off" })
            </div>
        </div>
        <div class="form-row">
            <div class="form-col">
                @Html.TextAreaFor(x => x.ValidationResult, new { rows = 10, cols = 200, wrap = "off" })
            </div>
        </div>
    </div>
</body>
</html>